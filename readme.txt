训练模型：
运行demo_Train_FDDL_Tensor.m这个文件。

预测模型：
运行demo_SR_FDDL_tensor.m这个文件。
若显示内存不足，则可以把这个文件中第19行的a=imresize(a,0.5,'bicubic');的注释去掉，把数据缩小一点。


这个是我电脑上的，模型优化都还没到最优的结果
训练数据由于有点大，我没放，但是我放了3张测试数据，要运行训练模型的话，训练的是3张图片的结果。